import { Meta } from "@storybook/react";
import { Section } from "./Section";

const meta = {
  title: "dotlearn/components/organism/Section",
  component: Section,
} satisfies Meta<typeof Section>;

export default meta;

export const Primary = {};
