import { Meta } from "@storybook/react";
import { Team } from "./Team";

const meta = {
  title: "dotlearn/components/organism/Team",
  component: Team,
} satisfies Meta<typeof Team>;

export default meta;

export const Primary = {};
