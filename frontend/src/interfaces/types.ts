export interface MenuProps {
  isActive: boolean;
}

export interface HeadingProps {
  firstLabel: string;
  secondLabel: string;
}

export interface Information {
  firstLabel: string;
  secondLabel: string;
  thirdLabel: string;
  description: string;
}
